kibana:
  imagePullSecrets:
  - name: private-registry-mil

  count: 1

elasticsearch:
  imagePullSecrets:
  - name: private-registry-mil

  master:
    count: 1
    resources:
      limits:
        cpu: 1
        memory: 3Gi

  data:
    count: 1
    persistence:
      storageClassName: ""
    resources:
      limits:
        cpu: 1
        memory: 3Gi

istio:
  enabled: true

bbtests:
  cypress:
    artifacts: true
    envs:
      
      cypress_url: "https://{{ Release.Name }}-es-http:9200"
    secretEnvs:
      - name: cypress_secretkey
        valueFrom:
          secretKeyRef:
            name: "{{ .Release.Name }}-es-elastic-user"
            key: password
     